<div class="pg-page container">
  <h1>{{ page.title }}</h1>
  <div class="rte">{{ page.content }}</div>
</div>

{% schema %}
{ "name": "Page générique", "tag": "section", "settings": [], "presets": [{ "name": "Page générique" }] }
{% endschema %}
